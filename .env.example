NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# API model configuration. The backend reads these through Docker Compose or the local process environment.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5
OPENAI_BASE_URL=

# Optional: point at a real notebook corpus. Leave blank to use the bundled sample notebook.
AEROAGENT_NOTEBOOK_ROOT=
